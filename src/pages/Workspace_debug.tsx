import Layout from "../components/Layout.tsx"; // ✅ Ensure Sidebar is used

const Workspace = () => {
  return (
    <Layout>
      <h1>Your Workspaces</h1>
      {/* Rest of your workspace UI */}
    </Layout>
  );
};

export default Workspace;
